$(document).ready(function () {
   $('.slider-work').slick({
      autoplay: false,
      arrows: false,
      // dots: true,
      // dotsClass: 'section-work__steps',
      // appendDots: $('.section-work__steps')
   });
});